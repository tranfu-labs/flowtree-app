services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "80"
    volumes:
      - flowtree-market-data:/app/data

volumes:
  flowtree-market-data:
